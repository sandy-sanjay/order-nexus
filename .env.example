# Example env for local development
# For local backend running on port 8080 (typical Spring Boot default):
REACT_APP_API_URL=http://localhost:8080

# For production, set this to your backend origin before running `npm run build`:
# REACT_APP_API_URL=https://your-backend.example.com
